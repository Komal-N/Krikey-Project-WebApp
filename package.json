{
  "scripts": {
    "dev": "npm run build && nodemon app.js",
    "build": "cd frontend && npm run build",
    "start": "npm run build && node app.js"
  },
  "devDependencies": {
    "dotenv": "^16.4.5",
    "nodemon": "^3.1.0"
  },
  "dependencies": {
    "express": "^4.19.2",
    "pg": "^8.11.5"
  }
}